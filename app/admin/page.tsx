"use client";
import { useEffect,useMemo,useState } from "react";
import { createClient } from "@/lib/supabase/client";
type Summary={users:number;family_accounts:number;subscriptions:number;failed_payments:number;professional_applications:number;reports_generated:number;open_account_requests:number};
export default function AdminPage(){const db=useMemo(()=>createClient(),[]),[summary,setSummary]=useState<Summary|null>(null),[error,setError]=useState("");useEffect(()=>{db.rpc("admin_dashboard_summary").then(({data,error})=>{if(error)setError("This dashboard is restricted to the registered Vitala owner account.");else setSummary(data as Summary)})},[db]);return <main className="admin-page"><a href="/app">← Back to Vitala Amanah</a><span className="eyebrow">OWNER ADMINISTRATION</span><h1>Vitala owner dashboard</h1><p>Operational metadata only. Administrators cannot open private documents or owner records.</p>{error&&<div className="error"><b>Owner access required</b><span>{error}</span></div>}{summary&&<div className="metrics">{Object.entries(summary).map(([key,value])=><section className="metric" key={key}><small>{key.replaceAll("_"," ")}</small><strong>{value}</strong></section>)}</div>}<section className="panel"><h2>Security boundary</h2><p>User documents, passwords, vault secrets and decrypted content are not queried by this dashboard.</p></section></main>}
