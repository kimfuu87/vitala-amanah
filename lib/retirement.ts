export type ProjectionInput={current_age:number;monthly_spending:number;current_savings:number;epf_balance:number;prs_balance:number;cash_investments:number;monthly_contribution:number;employer_contribution:number;inflation:number;life_expectancy:number;other_retirement_income:number;rental_income:number;vacancy_rate:number;property_expenses:number;healthcare_buffer:number;education_commitments:number;desired_legacy:number;debt_balance:number};
export const futureValue=(principal:number,monthly:number,annualRate:number,years:number)=>{const months=Math.max(0,years*12),rate=annualRate/1200;return rate===0?principal+monthly*months:principal*Math.pow(1+rate,months)+monthly*((Math.pow(1+rate,months)-1)/rate)};
export function retirementProjection(p:ProjectionInput,retirementAge:number,returnRate:number){const years=Math.max(0,retirementAge-p.current_age),starting=Number(p.current_savings)+Number(p.epf_balance)+Number(p.prs_balance)+Number(p.cash_investments),contribution=Number(p.monthly_contribution)+Number(p.employer_contribution),fund=futureValue(starting,contribution,returnRate,years),inflatedMonthly=Number(p.monthly_spending)*Math.pow(1+Number(p.inflation)/100,years),retirementYears=Math.max(1,p.life_expectancy-retirementAge),netIncome=Math.max(0,Number(p.other_retirement_income)+Number(p.rental_income)*(1-Number(p.vacancy_rate)/100)-Number(p.property_expenses)),need=Math.max(0,(inflatedMonthly-netIncome)*12*retirementYears+Number(p.healthcare_buffer)+Number(p.education_commitments)+Number(p.desired_legacy)+Number(p.debt_balance)),gap=need-fund,rate=returnRate/1200,months=years*12,annuity=rate===0?months:(Math.pow(1+rate,months)-1)/rate,required=gap>0&&annuity>0?gap/annuity:0;return{years,fund,need,gap,required,inflatedMonthly,netIncome}}
